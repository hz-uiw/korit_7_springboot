<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korit.springboot_study.mapper.BookMapper">

<!--  book_tb  -->
    <resultMap id="bookResultMap" type="com.korit.springboot_study.entity.Book">
        <id property="bookId" column="book_id"/>
        <result property="bookName" column="book_Name"/>
    </resultMap>

    <!--  도서정보 전체 조회  -->
    <select id="selectBooksAll" resultMap="bookResultMap">
        select
            bt.book_id,
            bt.book_name,
            bt.author_id,
            bt.isbn,
            bt.category_id,
            bt.publisher_id,
            bt.book_img_url

            at.author_id,
            at.author_name,

            ct.category_id,
            ct.category_name,

            pb.publisher_id,
            bt.publisher_name,
        from
            book_tb bt
            left outer join author_tb at on(at.author_id = bt.author_id)
            left outer join category_tb ct on(ct.category_id = bt.category_id)
            left outer join publisher_tb pb on(bt.publisher_id = bt.publisher_id)
    </select>

    <!--  도서 추가  -->
    <insert id="insertBook" keyProperty="bookId" useGeneratedKeys="true">
        insert into
            book_tb
        values
            (default, #{bookName})
    </insert>

</mapper>