<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.korit.springboot_study.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.korit.springboot_study.entity.User">
        <id property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name" />
        <result property="email" column="email"/>
        <collection property="userRoles" resultMap="userRoleResultMap" />
    </resultMap>

    <resultMap id="userRoleResultMap" type="com.korit.springboot_study.entity.UserRole">
        <id property="userRoleId" column="user_role_id"/>
        <result property="userId" column="user_id"/>
        <result property="roleId" column="role_id"/>
        <association property="role" resultMap="roleResultMap"/>
    </resultMap>

    <resultMap id="roleResultMap" type="com.korit.springboot_study.entity.Role">
        <id property="roleId" column="user_role_id"/>
        <result property="roleName" column="role_name"/>
    </resultMap>

    <select id="selectByUsername" resultMap="userResultMap">
        select
            user_id,
            username,
            password,
            name,
            email
        from
            user_tb
        where
            username = #{username}
    </select>


    <select id="selectById" resultMap="userResultMap">
        select
            ut.user_id,
            ut.username,
            ut.password,
            ut.name,
            ut.email,

            urt.user_role_id,
            urt.user_id,
            urt.role_id,

            rt.role_id,
            rt.role_name
        from
            user_tb as ut
            left outer join user_role_tb as urt on(urt.user_id = ut.user_id)
            left outer join role_tb as rt on(rt.role_id = urt.role_id)
        where
            ut.user_id = #{userId}
    </select>

    <insert id="insert" keyProperty="userId" useGeneratedKeys="true">
        insert into
            user_tb
        values
            (default, #{username}, #{password}, #{name}, #{email})
    </insert>
</mapper>